<div class="row" style="padding-top: 10px;">
  <div class="col"> </div>
    <div class="col-md-12">

      <h4>Directors</h4>

        <% if current_user && current_user.admin? %>

          <p>
            <%= link_to 'New Director', new_director_path, class: 'btn btn-lg btn-info' %>
          </p>
        <% end %>

        <table class="table table-hover">
          <thead>
            <tr class="table-light">
              <% if current_user && current_user.admin? %>
                <th>Name</th>
                <th>Description</th>
                <th colspan="3">Editing Options</th>

              <% else %>

                  <th>Name</th>
                  <th>Description</th>
                  <th colspan="1">Options</th>

              <% end %>
            </tr>
          </thead>

          <tbody>

            <% @directors.each do |director| %>

              <tr class="table-dark text-dark">
                  <td><%= director.name %></td>
                  <td><%= director.description %></td>
                  <td><%= link_to 'Show', director, class: 'btn btn-md btn-info' %></td>

                <% if current_user && current_user.admin? %>

                  <td><%= link_to 'Edit', edit_director_path(director), class: 'btn btn-md btn-warning' %></td>
                  <td><%= link_to 'Destroy', director, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-md btn-danger' %></td>

                <% end %>
              </tr>
            <% end %>
          </tbody>
        </table>

    </div>
  <div class="col"> </div>
</div>

