<%= render 'common/errors', model: @genre %>

<%= form_with(model: genre, local: true) do |f| %>

    <div class="field">
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
    
<% end %>
